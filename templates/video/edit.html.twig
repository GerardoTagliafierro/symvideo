{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-5">
  <div class="row">
    <div class="col-12">
      <h1>Edit</h1>
      {{form_start(form)}}
      {{form_widget(form)}}
      {{form_end(form)}}
      <a class="d-block mt-4" href="{{url('admin')}}"><- Back</a>
    </div>
  </div>
</div>
{% endblock %}